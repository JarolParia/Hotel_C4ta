<UserControl x:Class="Hotel_C4ta.View.AdminViews.UserManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="White" CornerRadius="10" Padding="20">
            
            <StackPanel>
                <TextBlock Text="👤 Gestión de Usuarios"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <DataGrid x:Name="UsersGrid"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          HeadersVisibility="Column"
          CanUserAddRows="False"
          Margin="0,10,0,0"
          RowHeight="35"
          FontSize="14"
          AlternatingRowBackground="#F0F0F0"
          GridLinesVisibility="None"
          BorderThickness="0"
          Background="White"
          SelectionMode="Single"
          SelectionUnit="FullRow">

                    <!-- Estilo de encabezados -->
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#4B0082"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Height" Value="40"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <!-- Columnas -->
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding _Id}" Width="60"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding _Name}" Width="*"/>
                        <DataGridTextColumn Header="Rol / Código" Binding="{Binding ExtraInfo}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>